# BobaOS lib

This library contains functions and routins speacial to the BobaOS userspace and it contains the startup routine for a C program.

## Startup
The startup code calls the main function of a C program and provide the default arguments.


**Every program that runs on BobaOS needs to include the BobaOSLib**


## Functions
